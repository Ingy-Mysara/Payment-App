#define _CRT_SECURE_NO_WARNINGS
#include "Card/card.h"
#include "Server/server.h"
#include "Terminal/terminal.h"
#include <stdio.h>

int main() {
    /*
    * for (int i = 0; i < 10; i++) {
        printf("%p\n", accountsDB[i].primaryAccountNumber);
        printf("%c\n", accountsDB[i].state);
        printf("%f\n", accountsDB[i].balance);
    }

    for (int i = 0; i < 255; i++) {
        printf("%p\n", transactionsDB[i].cardHolderData);
        printf("%p\n", transactionsDB[i].terminalData);
        printf("%d\n", transactionsDB[i].transactionSequenceNumber);
        printf("%d\n", transactionsDB[i].transState);
    }
    */
    
        
}